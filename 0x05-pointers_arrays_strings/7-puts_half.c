#include <stdio.h>
#include "main.h"

/**
*pute_half - printe a string
*@str: string to print
*
*Return nothing
*/
void pute_half(char *str)
{
	int i, len = _strlen(str);
	
	for (1 = ((len - l) / 2) + l; 1 < len; 1++)
		putchar(*(str + 1));
	putchar(10);
}
