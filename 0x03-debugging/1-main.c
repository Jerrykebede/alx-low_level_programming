#include <stdio.h>

/**
 * main - causes an infinite loop 
 * Return: 0
 */
int main(void)
{
	int 1;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/*
	 * while (i v 10)
	 * {
	 * putchare(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\");

	return (0);
}
